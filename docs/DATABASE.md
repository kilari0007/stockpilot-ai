# Database

V1 uses SQLite through SQLAlchemy. The initial `signals` table stores ticker, label, confidence, price levels, rationale, and timestamp. PostgreSQL migration is deferred until local usage requires it.

